{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "java -Xmx384m -jar /app/app.jar",
    "restartPolicyType": "NEVER",
    "healthcheckPath": "/actuator/health",
    "healthcheckTimeout": 300
  }
}